version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          yarn: true
          post-steps:
            - store_artifacts:
                path: mochawesome-report
            - store_artifacts:
                path: screenshots
            - store_artifacts:
                path: videos
